# SportsOS v0.3.5 Roster Engine

Copy this overlay over the root of the existing SportsOS v0.3.4 project.

Then rebuild:

```bash
docker compose build --no-cache api dashboard
docker compose up -d
```

Open **Rosters** in the dashboard, select an organization, season, and team, then add players.
